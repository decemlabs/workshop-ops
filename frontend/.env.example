# Адрес Django для прокси дев-сервера Vite (см. vite.config.ts).
BACKEND_URL=http://127.0.0.1:8000

# Префикс REST API. По умолчанию относительный /api — запросы уходят через прокси.
# Меняем, если фронт собирается под отдельный домен: VITE_API_URL=https://example.com/api
VITE_API_URL=/api
